# Legacy archive (2025)

本目录保存 Schema v2 页面升级后不再参与当前应用运行的旧文件。

| 文件 | 原位置 | 说明 |
| --- | --- | --- |
| `app1.py` | 项目根目录 | 旧版 Streamlit 页面实验稿 |
| `compounds-old.json` | `data/` | 旧化合物数据备份 |
| `methods-old.json` | `data/` | 旧方法数据备份 |

封存日期：2026-07-28。

当前应用入口为项目根目录的 `app.py`，运行时数据位于 `data/`。除非需要历史对比，不应从本目录加载文件。

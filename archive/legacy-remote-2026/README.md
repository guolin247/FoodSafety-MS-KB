# Legacy remote archive (2026)

本目录保存在同步 `origin/main` 时发现、但不再参与 Schema v2 应用运行的旧版内容。

## 目录

- `runtime-v1/`：远程旧版 `README.md`、`app.py` 和三份旧运行数据。
- `extraction_processing/`：旧版数据提取与清洗管线。
- `validation_scripts/`：面向旧数据结构的验证脚本和样例数据。

归档来源：`origin/main` 提交 `eaacdd6`。

归档日期：2026-07-28。

当前应用入口、Schema v2 数据加载器和测试均位于项目根目录；本目录仅用于历史追溯。
